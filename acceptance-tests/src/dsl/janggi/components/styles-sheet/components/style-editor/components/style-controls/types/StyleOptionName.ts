/**
 * The buttons of the style editor's choices that a spec presses, spelled out for the reason
 * `StyleControlName` is: `<choice>-<option>`, as the editor's test ids read.
 */
export type StyleOptionName =
  | "writing-Hanja"
  | "writing-Hangul"
  | "glyph-kind-character"
  | "glyph-kind-pictograph"
  | "body-shape-octagon"
  | "body-shape-disc";
