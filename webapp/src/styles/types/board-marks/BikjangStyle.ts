/** How the line down the open file between two generals is drawn once a bikjang has been called. */
export interface BikjangStyle {
  /** Any CSS colour. */
  readonly colour: string;
  /** In pixels, whatever size the board is drawn at. */
  readonly width: number;
}
