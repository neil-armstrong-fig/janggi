import {TOUR_STEP_NAMES} from "@src/redux/onboarding/touring/TourStepName";

/** How many steps the tour has, which is where its last one is. */
export const TOUR_STEP_COUNT = TOUR_STEP_NAMES.length;
