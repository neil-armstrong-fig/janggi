/**
 * Where the preferences are kept in `localStorage`. Versioned, so a later change to their shape can be
 * read under a new key rather than misread under this one.
 */
export const PREFERENCES_STORAGE_KEY = "janggi.preferences.v1";
