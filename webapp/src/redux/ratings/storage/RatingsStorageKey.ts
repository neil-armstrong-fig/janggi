/**
 * Where the ratings are kept in `localStorage`. Versioned, so a later change to their shape can be
 * read under a new key rather than misread under this one.
 */
export const RATINGS_STORAGE_KEY = "janggi.ratings.v1";
