/**
 * Where the player's own styles are kept in `localStorage`. Versioned, so a later change to their shape
 * can be read under a new key rather than misread under this one.
 */
export const CUSTOM_STYLES_STORAGE_KEY = "janggi.custom-styles.v2";
