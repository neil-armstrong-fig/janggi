/**
 * How a held note is shaped: played plain, scooped into from below, or let fall away at its end — the
 * ornaments (시김새) that make a slow reed line sing rather than sit.
 */
export type Slide = "none" | "scoop" | "fall";
