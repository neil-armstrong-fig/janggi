/** A long note the answering flute holds. */
export interface AnswerNote {
  /** A degree of 평조, counted up from the flute's root. */
  readonly degree: number;
  /** How many steps it is held for. */
  readonly steps: number;
}
