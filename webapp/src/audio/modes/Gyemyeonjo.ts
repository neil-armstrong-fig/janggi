import type {Mode} from "@src/audio/modes/types/Mode";

/**
 * 계면조 — the plaintive mode, heard here as a minor pentatonic. The check theme is in it, so a
 * general under attack is heard as the music turning dark rather than only getting louder.
 */
export const GYEMYEONJO: Mode = {name: "gyemyeonjo", steps: [0, 3, 5, 7, 10]};
